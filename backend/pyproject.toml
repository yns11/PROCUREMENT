[project]
name = "procurement-app"
version = "2.0.0"
description = "PROCUREMENT – supply planning cockpit & MRP simulation engine (Databricks App)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
